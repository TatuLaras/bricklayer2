#include "gfx.h"
#include "graphics_api.h"

void gfx_init(GfxInitInfo *info) {
    gapi_init(info);
    // Init window
    // Select physical device
    // create swapchain, logical device, depth etc.
}
